# Changelog

本文件记录 `klay` 个人主站的重要更新。普通文章内容更新、错别字修正和很小的样式调整通常不需要记录。

## 2026-05-24

### Added

- 新增首页布局配置，支持通过 `content/home-sections.json` 控制首页区块顺序、开关和参数。
- 新增后台「首页布局」管理能力，可调整首页区块、展示数量和自定义内容。
- 新增自定义 Markdown 首页区块，用于在首页插入自由内容。
- 新增本地后台写文件服务，开发环境下可从 `/admin` 直接保存内容到仓库文件。
- 新增线上后台保存能力，可通过 GitHub PAT 把内容修改提交到指定仓库和分支。
- 新增 `sitemap.xml`、`robots.txt` 和 `rss.xml`，方便搜索引擎收录和 RSS 订阅。
- 新增文章发布前检查，保存前提示标题、日期、摘要、标签、封面路径和正文问题。
- 新增文章详情页目录和上一篇 / 下一篇导航。
- 新增项目详情页封面、更新时间和项目状态说明。
- 新增页面底部 RSS 入口。

### Changed

- 首页改为由配置驱动，区块不再固定写死在页面中。
- 个人信息、技能、经历、项目等核心内容改为优先通过 JSON 文件维护。
- 博客和项目内容维护流程整理为当前 README 中的开发使用文档。
- README 扩展为完整项目说明，覆盖本地开发、后台使用、内容维护、构建和部署流程。
- Markdown 渲染不再放行原始 HTML，文章和自定义区块只渲染安全的 Markdown 输出。

### Fixed

- 修复后台文章列表中删除文章后，已删除文章仍留在页面列表里的问题。
- 修复 admin 页面阻塞 ESLint 的状态更新、类型和内部链接问题。
- 限制本地后台文章 slug 和图片上传文件名，避免异常路径写入。
- 修复文章删除依赖浏览器原生确认框的问题，改为站内二次确认并要求输入 slug。

## 更新记录维护规则

- 改功能：记录到本文件。
- 改使用方式或开发流程：同时更新 `README.md` 和本文件。
- 改文章内容：一般不需要记录。
- 改部署流程：同时更新 `README.md` 和本文件。
- 未来如果开始正式打版本，可在 GitHub Releases 中复用本文件内容。
